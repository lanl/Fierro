{% set version = "1.0.0" %}
{% set linux_compiler_version = "10.4.0" %}
{% set macos_compiler_version = "12" %}

package: 
  name: fierro-voxelizer-py
  version: 2

source:
  path: ../../
  # - git_url: https://github.com/lanl/Fierro.git
  #   depth: 1

build:
  skip: true  # [ (not linux64) or (not osx) ]
  skip: false # [ linux64 or osx ]

requirements:
  build:
    - cmake >=3.17.0
    - {{ compiler('cxx') }}={{ linux_compiler_version }} # [linux]
    - {{ compiler('cxx') }}={{ macos_compiler_version }} # [osx]
  host:
    - elements
    - python
    - pybind11
    - setuptools
  run:
    - python

test:
  imports:
    - fierro_voxelizer
